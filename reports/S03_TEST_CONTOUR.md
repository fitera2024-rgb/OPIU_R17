# S03 — единый тестовый контур R005/R001

Дата проверки: `27.08.2026`

## Контракт

- Версия: `0.4`
- Файл: `contracts/Контракт_ОПИУ_v0.4_зафиксированный.docx`
- SHA-256: `09AB635802E436C2C33E2FD39D8B35E62631376AB9AE8DA6F6EFC23EAF844BCD`
- Применимые разделы: 1, 4–14, 16 и приложение B.

## Инвентарь

- R005: `36` тестовых файлов = `31` точных тестов R16 + `5` сохранённых baseline-only.
- R001: `25` тестовых файлов = `1` свежий тест R16 + `24` сохранённых baseline.
- Все 31 тест R005 из R16 и тест `r001_group_scoped_posting_rule.test.mjs` совпадают с источником по SHA-256.
- Удалённых baseline-тестов: `0`.
- Production MJS, UI, справочники и пользовательские настройки задачей S03 не изменялись.
- `git diff --check`: `PASS`.

## Legacy-аудит Rules Service

Два сохранённых R001-теста импортируют исторические модули `modules/rules-engine`:

- `disputed_rules_to_r001.test.mjs`;
- `r001_handoff_input.test.mjs`.

Они сохраняются только как legacy-аудит совместимости. Их наличие не разрешает runtime Rules Service. Инвариант остаётся `rules_service=false`.

## Прогон Node 24

Среда: Node `v24.19.0`. Зависимости `jszip` и `@oai/artifact-tool` разрешались из проверенного runtime R16 только для QA; `node_modules` не копировался и не добавлялся в репозиторий.

### R005

- Успешных test cases: `194`.
- Уникальных падений: `4`.
- Статус: `FAIL`, требуется разбор совместимости.

Падения:

1. `profile_detector_12_test.mjs`: свежий тест ожидает 13 разрешённых корневых профилей, текущий `organization_profiles.json` содержит 12 разрешённых; R16-справочник новее текущего R17.
2. `sakhalin_operation_evidence_test.mjs`: data-driven acceptance требует путь к извлечённому журналу Сахалина как аргумент 1; это не расчётная ошибка.
3. `service_r005_owner_wrapper_inventory.test.mjs`: baseline-only тест ожидает export `authoritativeStructuralInventoryHierarchyPeriodsFromPayload`, отсутствующий в текущем wrapper R16.
4. `structural_control_report_detail.test.mjs`: baseline-only тест проверяет прежнюю точную форму вызова `buildStructuralControlReportDetail`; текущий production использует другую связку.

### R001

- Успешных test cases: `191`.
- Уникальных падений: `49`.
- Статус: `FAIL`, нельзя исправлять ослаблением физического доказательства.

Основные группы:

- отсутствующие/переименованные exports production API;
- прежние имена файлов и формат поля `Содержание`;
- старые ожидания знака STORNO;
- старые workbook/registry contracts;
- ожидание финансовых A:AA-строк при неполной физической ERP-идентичности.

Последняя группа требует проверки по контракту: READY/STORNO/REPOST требует уникальную физическую строку ERP, но доказанное направление при неполной физической идентичности должно оставаться видимым как `СПОРНО` с пустыми недоказанными полями, а не исчезать.

## Решение координатора

- Инвентарь S03: `PASS`.
- Полная приёмка S03: `BLOCKED`, пока каждое падение не классифицировано как устаревший тест, fixture или реальная регрессия.
- Production нельзя ослаблять ради старого теста.
- Тест нельзя удалять или переписывать без сопоставления с контрактом v0.4.
- S06 выполняется параллельно в отдельных файлах и не входит в этот diff.

QA-логи текущего прогона находятся локально в `.qa/s03-r005.tap` и `.qa/s03-r001.tap` и не включаются в Git.
