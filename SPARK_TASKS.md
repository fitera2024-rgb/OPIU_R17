# Очередь задач Codex Spark

Статусы: `READY`, `RUNNING`, `REVIEW`, `MERGED`, `BLOCKED`.

Новые задачи выдаёт координатор. Запуск выполняет пользователь. Задача без текущего контракта блокируется.

| ID | Статус | Область | Цель |
|---|---|---|---|
| S00 | MERGED | Репозиторий | Чистый baseline и независимый аудит отличий R16 |
| S01 | MERGED | R005/R001 | Production-реализация R16, commit `8a63606a` |
| S02 | MERGED | Справочники | 21 физический ref, legacy Rules Service data удалён, commit `19822da4` |
| S03 | READY | Тесты | Объединить свежие тесты R16 и полный применимый контур R001 из исходного репозитория |
| S04 | READY | Утверждения: ядро | `01_Правила`, XLSX round-trip, проверки и versioned approved JSON |
| S05 | BLOCKED | Утверждения: UI | Отдельное окно, пять действий и безопасное массовое утверждение; после S04 |
| S06 | READY | Launcher/порт | Безопасно завершить только прежний OPIU, дождаться освобождения порта и запустить один новый экземпляр |
| C01 | RUNNING | Координация | Контракт v0.4, review S04/S05, A17–A24 и интеграционная приёмка |

## Шаблон карточки

```text
ID и одна цель:

Контракт:
- файл: contracts/Контракт_ОПИУ_v0.4_зафиксированный.docx
- версия: 0.4
- SHA-256: 09AB635802E436C2C33E2FD39D8B35E62631376AB9AE8DA6F6EFC23EAF844BCD
- применимые разделы:

Входы:
Разрешено изменять:
Запрещено:
Шаги:
PASS:
Отчёт:
```

## S01 — импорт production-реализации R16

```text
ID и одна цель:
S01. Перенести поверх чистого baseline только production-файлы R005/R001 из финального R16, не удаляя отсутствующие в пакете baseline-файлы.

Контракт:
- прочитать AGENTS.md и contracts/CURRENT.md до первой мутации;
- файл: contracts/Контракт_ОПИУ_v0.2_зафиксированный.docx;
- версия: 0.2;
- SHA-256: 4C64998B675B6D0F910DA557CE1CBE20C5E15A023C75830CC145FC95DA6B540A;
- разделы: 1, 4–8, 10, 13–16 и приложение B.

Входы:
- репозиторий: C:\Users\NB-FIT\Documents\OPIU_R17;
- подтверждённый пакет: C:\Users\NB-FIT\Documents\Codex\2026-08-25\c-users-nb-fit-documents-chatgpt\outputs\OPIU_R16;
- manifest: PACKAGE_MANIFEST.json, SHA-256 EE7128280F90207CE3EBA10DBF9FF61A8C33230817A807F10B250EF4731A5EC4.

Разрешено изменять:
- modules/reconciliation/source/*.mjs;
- modules/corrections/source/*.mjs.

Запрещено:
- удалять baseline-файлы, которых нет в R16;
- менять PowerShell/UI, справочники, user-settings, контракт и документы координации;
- переносить node_modules, work, outputs, results, *.log, EXE, ZIP и data/ui-context;
- возвращать Rules Service;
- выполнять git commit.

Шаги:
1. Сверить входной manifest и его SHA-256.
2. Наложить runtime/modules/reconciliation/source и runtime/modules/corrections/source поверх одноимённых каталогов R17 только для production MJS; явно исключить `*.test.mjs` и `*_test.mjs`.
3. Не удалять дополнительные baseline-тесты.
4. Показать git diff --stat, перечень новых/изменённых файлов и git diff --check.

PASS:
- ключевой modules/reconciliation/source/opiu_reconcile.mjs имеет SHA-256 AE9477886F8C8D8E88CC60BA889E9D652209E4AB3EBAE2BDD7FB0DCBB411F82E;
- modules/corrections/source/correction_engine_r001.mjs имеет SHA-256 EF2F8E314482E9EE9E127FFF863CCE43623872795673D91F80578E67141E2546;
- добавлены journal-first и self-discovery production-модули R16;
- нет удалений baseline-файлов;
- запрещённых путей в git status нет;
- git diff --check не сообщает ошибок.

Отчёт:
- статус PASS/FAIL;
- изменённые и новые файлы;
- фактические SHA ключевых файлов;
- команды проверки и полный вывод ошибок, если они есть;
- коммит не создавать.
```

## S02 — канонические справочники без junction и legacy Rules Service

```text
ID и одна цель:
S02. Заменить старые/junction-зависимые справочники R17 физическими проверенными файлами R16 и удалить legacy data/defaults старого Rules Service.

Контракт:
- прочитать AGENTS.md и contracts/CURRENT.md до первой мутации;
- файл: contracts/Контракт_ОПИУ_v0.2_зафиксированный.docx;
- версия: 0.2;
- SHA-256: 4C64998B675B6D0F910DA557CE1CBE20C5E15A023C75830CC145FC95DA6B540A;
- разделы: 1, 3–6, 9, 13–16 и приложение B.

Входы:
- репозиторий: C:\Users\NB-FIT\Documents\OPIU_R17;
- источник: C:\Users\NB-FIT\Documents\Codex\2026-08-25\c-users-nb-fit-documents-chatgpt\outputs\OPIU_R16\runtime;
- manifest R16 SHA-256: EE7128280F90207CE3EBA10DBF9FF61A8C33230817A807F10B250EF4731A5EC4.

Разрешено изменять:
- modules/reconciliation/source/external_reference/**;
- modules/reconciliation/source/resources/**;
- modules/reconciliation/source/config.json;
- modules/reconciliation/source/r005_intalev_template_graph.current.json;
- modules/reconciliation/source/reference_catalog_manifest.current.json;
- resources/reference/**;
- data/defaults/** только для удаления legacy;
- отдельный manifest SHA справочников.

Запрещено:
- менять *.mjs, тесты, UI, контракт и user-settings;
- переносить work, results, logs, cache, drafts, EXE, ZIP, node_modules, data/ui-context;
- переносить rules.json, runs.json, approvals и состояние старого Rules Service;
- выполнять git commit.

PASS:
- все обязательные справочники R16 физически присутствуют и являются обычными файлами, не junction;
- присутствует resources/reference/ОрганизациииерархияЕРП.xlsx;
- manifest справочников и все SHA проходят;
- legacy data/defaults удалён из tracked-файлов;
- отсутствующий optional intalev_articles.xlsx не маскируется и обозначен как optional;
- git diff --check PASS, запрещённых путей в status нет.

Отчёт:
- PASS/FAIL, новые/изменённые/удалённые файлы;
- manifest и SHA;
- список намеренно отсутствующих optional refs;
- коммит не создавать.
```

## S03 — единый тестовый контур R005/R001

```text
ID и одна цель:
S03. Объединить свежие тесты R005 из финального R16 с полным baseline-контуром R001, не возвращая runtime Rules Service и не меняя production-код.

Контракт:
- прочитать AGENTS.md и contracts/CURRENT.md до первой мутации;
- файл: contracts/Контракт_ОПИУ_v0.4_зафиксированный.docx;
- версия: 0.4;
- SHA-256: 09AB635802E436C2C33E2FD39D8B35E62631376AB9AE8DA6F6EFC23EAF844BCD;
- разделы: 1, 4–14, 16 и приложение B;
- UI-001/A16 относится к следующей UI-задаче: в S03 требуется зафиксировать отсутствие такого теста, но launcher не изменять.

Входы:
- репозиторий: C:\Users\NB-FIT\Documents\OPIU_R17;
- свежие тесты R16: C:\Users\NB-FIT\Documents\Codex\2026-08-25\c-users-nb-fit-documents-chatgpt\outputs\OPIU_R16\runtime;
- полный baseline R001: C:\Users\NB-FIT\Documents\ChatGPT\OPIU_ST_integration_0825\development\OPIU_1.9.4;
- contract v0.4 SHA-256: 09AB635802E436C2C33E2FD39D8B35E62631376AB9AE8DA6F6EFC23EAF844BCD.

Разрешено изменять:
- только `modules/reconciliation/source/*.test.mjs` и `modules/reconciliation/source/*_test.mjs`;
- только `modules/corrections/source/*.test.mjs` и `modules/corrections/source/*_test.mjs`;
- отдельный test inventory/report без runtime-артефактов.

Запрещено:
- менять production MJS, PowerShell/UI, справочники, контракт, user-settings и package-файлы;
- переносить Rules Service, `data/defaults`, work, results, logs, cache, node_modules, EXE или ZIP;
- удалять baseline-only тест без отдельного письменного обоснования;
- выполнять git commit.

Шаги:
1. Для R005 наложить 31 тест R16 и сохранить пять baseline-only тестов: `owner_nested_parent_invariants.test.mjs`, `service_r005_owner_wrapper_inventory.test.mjs`, `structural_control_authoritative_candidates.test.mjs`, `structural_control_current_hierarchy_binding.test.mjs`, `structural_control_report_detail.test.mjs`.
2. Для R001 наложить свежий `r001_group_scoped_posting_rule.test.mjs` из R16 и сохранить остальные baseline-тесты.
3. Отдельно перечислить тесты, импортирующие `modules/rules-engine`; они являются legacy-аудитом и не доказывают разрешение Rules Service в runtime.
4. Запустить все R005 и R001 тесты Node 24 с `--test --test-force-exit`.
5. Зафиксировать количество файлов и тест-кейсов, время, exit code и полный перечень ошибок.

PASS:
- R005 test files: 36; R001 test files: 25 до отдельного решения об удалении legacy-аудитов;
- все свежие тесты R16 присутствуют с точными SHA-256 источника;
- пять baseline-only R005 и полный baseline R001 не потеряны;
- production MJS diff относительно commit `8a63606a` отсутствует;
- тесты завершаются самостоятельно, exit code 0, failed 0;
- нет runtime Rules Service state и запрещённых путей;
- `git diff --check` PASS.

Отчёт:
- PASS/FAIL;
- изменённые и новые тесты с источником и SHA-256;
- количество test files/test cases/passed/failed;
- legacy Rules Service imports отдельным списком;
- UI-001/A16 отмечен как обязательный будущий launcher regression test;
- коммит не создавать.
```

## S04 — ядро пользовательского утверждения статей

```text
ID и одна цель:
S04. Реализовать полный безопасный round-trip утверждений: движок создаёт лист 01_Правила, принимает заполненный лист обратно, валидирует решения и атомарно сохраняет новую approved-версию для точной организации и периода.

Контракт:
- до первой мутации прочитать AGENTS.md и contracts/CURRENT.md;
- файл: contracts/Контракт_ОПИУ_v0.4_зафиксированный.docx;
- версия: 0.4;
- SHA-256: 09AB635802E436C2C33E2FD39D8B35E62631376AB9AE8DA6F6EFC23EAF844BCD;
- разделы: 6, 9.1–9.6, 12.1, 13, 14; критерии A17–A22; приложение B.

Входы:
- репозиторий: C:\Users\NB-FIT\Documents\OPIU_R17;
- текущий production R005/R001 и объединённый тестовый контур после S03;
- XLSX сверки текущего запуска, организация верхнего уровня ERP и период ГГГГ-ММ;
- каталоги статей ERP и физический журнал ERP используются только как доказательства, не как скрытый Rules Service.

Разрешено изменять:
- новые изолированные модули `modules/reconciliation/source/article_approval_*.mjs` и их тесты;
- минимальные интеграционные точки R005, необходимые для создания `01_Правила` и чтения approved-настроек;
- backend-файлы `service/source/article_approvals*.go`, их тесты и только необходимые API/pipeline bindings;
- тестовые fixtures без персональных и production-данных.

Запрещено:
- менять HTML/CSS/браузерный UI — это S05;
- менять бизнес-логику R001, создавать готовую проводку без уникальной физической строки ERP или ослаблять REPORT_ONLY;
- возвращать Rules Service, `data/defaults`, скрытые rules.json или глобальное соответствие без организации/периода;
- перезаписывать предыдущий approved-файл, принимать неизвестное шестое решение или сохранять частичную версию;
- писать production approved-файлы во время тестов;
- выполнять git commit.

Шаги:
1. Сформировать `01_Правила` в том же XLSX текущей сверки с точными колонками §9.6 и SHA-256 источника.
2. Начально ставить `ПРЕДЛОЖЕНО ДВИЖКОМ` только для единственной валидной ERP-цели; остальные строки — `НУЖНА ПРОВЕРКА`.
3. Принять ровно пять решений; для `ИЗМЕНИТЬ` требовать блок, статью, код и комментарий.
4. Проверить конфликт целей, существование ERP-кода, принадлежность блоку, организацию, ПериодС и SHA исходной сверки.
5. При любой ошибке вернуть построчную диагностику и не создать частичный approved-файл.
6. Атомарно создать `user-settings/approvals/article_registry_<organization_slug>_vNNN.approved.json` и отдельную SHA-256; не менять старые версии.
7. В следующем запуске применить последнюю валидную версию только в точной области; `ЗАПРЕТИТЬ` блокирует связь, два неутверждённых статуса правилом не становятся.
8. Сохранить финансовый барьер: approved-соответствие без уникальной физической строки ERP даёт только `СПОРНО`, 0 готовых STORNO/REPOST.

PASS:
- A17–A22 имеют отдельные автоматические позитивные и негативные тесты;
- v001 остаётся байт-в-байт неизменным после создания v002;
- повреждённый SHA, чужая организация/период и неизвестный код отвергаются;
- ни один старый тест R005/R001 не потерян и все применимые тесты завершаются с exit code 0;
- `rules_service=false`, `REPORT_ONLY=true`, готовых финансовых строк без физической ERP-строки 0;
- `git diff --check` PASS, запрещённых путей в status нет.

Отчёт:
- PASS/FAIL по A17–A22;
- список файлов и назначение каждого;
- схема API/CLI и approved JSON без пользовательских данных;
- команды тестов, test files/cases/passed/failed и полный текст ошибок;
- коммит не создавать.
```

## S05 — интерфейс утверждения статей

Статус `BLOCKED` до объединения S04 координатором.

```text
ID и одна цель:
S05. Добавить отдельное понятное окно утверждений поверх готового API S04: очередь НУЖНА ПРОВЕРКА, пять действий, изменение цели, фиксация и безопасное массовое утверждение однозначных строк.

Контракт:
- до первой мутации прочитать AGENTS.md и contracts/CURRENT.md;
- файл: contracts/Контракт_ОПИУ_v0.4_зафиксированный.docx;
- версия: 0.4;
- SHA-256: 09AB635802E436C2C33E2FD39D8B35E62631376AB9AE8DA6F6EFC23EAF844BCD;
- разделы: 9.5–9.6, 11, 13, 14; критерии A23–A24; приложение B.

Предусловие:
- S04 объединена и её API/schema/tests зелёные; координатор переводит карточку в READY.

Разрешено изменять:
- `service/source/web/index.html`, `styles.css` и новый `article-approval-ui.js`;
- минимальную регистрацию скрипта/навигации в существующем `app.js`;
- `service/source/web_tests/article-approval-ui.test.cjs` и изолированные browser fixtures.

Запрещено:
- менять расчёт R005/R001, approved-схему, backend S04, правила финансовых проводок и launcher;
- скрыто утверждать строки при открытии окна;
- включать `НУЖНА ПРОВЕРКА`, конфликтные или невалидные строки в массовое утверждение;
- выполнять git commit.

PASS:
- A23: видимы очередь, пять действий, поля `ИЗМЕНИТЬ`, построчные ошибки, пользователь/область и имя созданной версии;
- A24: массовая кнопка меняет только строки с единственной валидной ERP-целью; остальные остаются `НУЖНА ПРОВЕРКА`;
- кнопка фиксации недоступна при обязательных ошибках и не показывает успех без ответа backend;
- интерфейс не создаёт проводок и явно объясняет, что утверждается связь статей, а не сумма;
- web-тесты и прежние UI-тесты PASS; `git diff --check` PASS.

Отчёт:
- PASS/FAIL A23–A24;
- список файлов;
- команды и результаты тестов;
- скриншоты основных и ошибочных состояний;
- коммит не создавать.
```

## C01 — задача координатора

```text
ID и одна цель:
C01. Зафиксировать контракт v0.4 и безопасно объединить S03–S05, доказав полный цикл утверждения без регрессий сверки и без финансовых строк, созданных только по догадке.

Ответственный: основной Codex-координатор.

Обязанности:
1. Проверить каждый diff на соответствие текущему SHA контракта и разрешённым файлам.
2. Не объединять S05 до зелёного S04; не давать двум исполнителям один файл одновременно.
3. Запустить A01–A24, полный R005/R001, Go/web-тесты и `git diff --check`.
4. Проверить октябрь 2025, ноябрь 2025 и Сахалин январь 2025: блоки не исчезают, дельта раскрывается, строки журнала не переиспользуются.
5. Отдельно проверить быстрый повторный запуск UI-001/A16 до сборки EXE.
6. Зафиксировать коммиты, теги, SHA и протокол только после PASS; при новой ошибке сначала обновить ERROR_REGISTER и при необходимости контракт.

PASS:
- APPROVAL-001 закрыта доказанным XLSX → approved JSON → повторный запуск round-trip;
- UI-001/A16 и A17–A24 PASS;
- `rules_service=false`, `REPORT_ONLY=true`;
- без уникальной физической ERP-строки готовых STORNO/REPOST нет;
- статус, commit/tag и оставшиеся вопросы отражены в STATUS.md.
```

## S06 — безопасный быстрый повторный запуск

```text
ID и одна цель:
S06. Перед каждым запуском освободить точный локальный порт только от подтверждённого предыдущего экземпляра OPIU, дождаться освобождения и запустить ровно один новый экземпляр.

Контракт:
- до первой мутации прочитать AGENTS.md и contracts/CURRENT.md;
- файл: contracts/Контракт_ОПИУ_v0.4_зафиксированный.docx;
- версия: 0.4;
- SHA-256: 09AB635802E436C2C33E2FD39D8B35E62631376AB9AE8DA6F6EFC23EAF844BCD;
- разделы: 11, 13, 14; критерий A16; ошибка UI-001; приложение B.

Разрешено изменять:
- `service/source/main.go`, `existing_service.go` и новые платформенные файлы `port_owner_*.go`;
- только относящиеся к preflight/повторному запуску Go-тесты;
- диагностическое сообщение и локальный журнал запуска.

Запрещено:
- менять R005/R001, HTML/JS/CSS, API утверждений, справочники, контракт и packaging;
- завершать процесс только по совпадению номера порта;
- завершать посторонний процесс, если identity/путь/PID предыдущего OPIU не доказаны;
- открывать браузер или рабочий файл до готовности нового экземпляра;
- оставлять два процесса или зависший индикатор после формирования результата;
- выполнять git commit.

Шаги:
1. Определить точный адрес/порт текущего пакета и проверить владельца listener.
2. Если listener принадлежит предыдущему OPIU, безопасно завершить его дерево и записать PID/путь/причину в локальную диагностику.
3. Если владелец чужой или не доказан, ничего не завершать и заблокировать запуск понятным сообщением с портом, PID и путём к журналу.
4. Дождаться реального освобождения порта с ограниченным тайм-аутом; только затем создать один новый listener и открыть интерфейс.
5. После закрытия/формирования результата убедиться, что порт снова свободен и дерево процесса завершено.

PASS:
- A16: занятый прежним OPIU порт освобождён, старое дерево завершено, запущен ровно один новый процесс;
- чужой тестовый listener остаётся жив, новый OPIU не стартует и показывает точную причину;
- быстрые двойной и тройной запуск не подключаются к старому процессу и не создают гонку;
- тайм-аут сообщает порт, PID и диагностический журнал;
- все существующие Go-тесты и новые Windows/other tests PASS; `git diff --check` PASS.

Отчёт:
- PASS/FAIL A16 и негативного теста чужого listener;
- список файлов;
- PID/port/process-tree протокол без персональных данных;
- команды и результаты тестов;
- коммит не создавать.
```
